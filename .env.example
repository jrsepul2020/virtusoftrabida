### Example environment variables for local development
# Copy this file to .env.local and fill values. Do NOT commit real secrets.

# Supabase (public client keys used by frontend)
# Get from: https://supabase.com/dashboard → Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Server-side (Service Role) - KEEP SECRET, set in deployment platform only
# Required for MCP servers (.mcp.json) and serverless functions
# Get from: https://supabase.com/dashboard → Project Settings → API → service_role
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Database connection (optional for scripts using direct PG client)
# DATABASE_URL=postgresql://user:password@host:5432/database

# Brevo (email)
# BREVO_API_KEY=your-brevo-api-key
# SENDER_EMAIL=info@internationalvirtus.es
# SENDER_NAME=International Virtus

# Admin access tokens (DO NOT store real secrets in repo)
# ADMIN_ACCESS_SECRET=your_admin_secret
# ADMIN_ACCESS_TOKEN=your_admin_token

# Claude Code / Anthropic API (for AI-assisted development)
# Get your key from: https://console.anthropic.com/settings/keys
# ANTHROPIC_API_KEY=sk-ant-api03-...

# Notes:
# - Never commit real API keys or service role keys into source control.
# - For production, configure secrets in your hosting provider (Vercel, Supabase, etc.)
# Variables de entorno necesarias para el proyecto

# Supabase
VITE_SUPABASE_URL=tu_supabase_url_aqui
VITE_SUPABASE_ANON_KEY=tu_supabase_anon_key_aqui

# Brevo (para emails)
BREVO_API_KEY=tu_brevo_api_key_aqui
SENDER_EMAIL=no-reply@virtuslarabida.com
SENDER_NAME=International Virtus La Rábida

# PayPal
VITE_PAYPAL_CLIENT_ID=tu_paypal_client_id_aqui

# Sentry (monitorización)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=production
VITE_SENTRY_RELEASE=
VITE_SENTRY_TRACES_SAMPLE_RATE=0.1
VITE_SENTRY_REPLAYS_SESSION_SAMPLE_RATE=0
VITE_SENTRY_REPLAYS_ON_ERROR_SAMPLE_RATE=0.1

# Para subir sourcemaps (opcional en CI/CD)
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_RELEASE=